Square icon-only control for toolbars, theme switch, avatar-adjacent actions. Always pass `label` for a11y (renders as aria-label + title).

```jsx
<IconButton icon={<Sun size={18}/>} label="Cambiar tema" active={false} />
```

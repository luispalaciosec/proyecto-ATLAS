Loading placeholder bar. Add the keyframes once globally (see tokens/base.css) — `@keyframes atlas-skeleton{0%{background-position:200% 0}100%{background-position:-200% 0}}`.

```jsx
<Skeleton width={180} height={14} />
```

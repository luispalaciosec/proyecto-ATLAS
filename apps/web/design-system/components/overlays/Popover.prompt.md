Anchored dropdown surface — wraps a `Listbox` for the brand switcher and similar menus.

```jsx
<div style={{position:"relative"}}><Popover open><Listbox items={["General","Banco Machala"]} /></Popover></div>
```

Dropdown brand switcher used inside TopBar. "General" is a real, default context — treat it like any brand, not a placeholder.

```jsx
<BrandSelector brands={["General","Banco Machala"]} selected="General" />
```

Large-but-compact input for the ATLAS workspace chat — no giant floating chatbot bubble; this is a workspace tool, not a consumer chat widget.

```jsx
<ChatComposer value={text} onChange={e=>setText(e.target.value)} onSend={send} />
```

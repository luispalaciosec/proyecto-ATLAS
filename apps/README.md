# Applications

Executable applications that consume the Atlas platform.

Examples defined in ATLAS-ARCH-002:

- atlas-cli
- atlas-api
- atlas-web
- atlas-studio
- atlas-vscode

Applications SHALL consume the platform. They never implement it.

The CLI package lives at `packages/cli/` per SDK-201.

Application implementations will be added in future phases.

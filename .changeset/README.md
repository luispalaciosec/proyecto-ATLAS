# Changesets

Atlas uses [Changesets](https://github.com/changesets/changesets) for version management across the monorepo.

## Usage

```bash
pnpm changeset
pnpm version-packages
pnpm release
```

Each package follows Semantic Versioning as defined in ATLAS-ARCH-002.
